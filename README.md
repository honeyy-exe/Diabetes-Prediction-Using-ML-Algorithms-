# Diabetes-Prediction-Using-ML-Algorithms-
This project uses Python, NumPy, and an SVM classifier to predict diabetes from medical inputs. Feed the data, let the scaler normalize your life, and let the model decide your fate.
